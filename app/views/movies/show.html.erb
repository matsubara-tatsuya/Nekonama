<h2>タイトル</h2>
<p><%= @movie.room_name %></p>
<iframe width="560" height="315" src="<%= @movie.room_name %>autoplay=1 " frameborder="0" allowfullscreen></iframe>
<h2>詳細</h2>
<p><%= @movie.detail %></p>

<!-- editアクションへのリンクを作成-->
<%= link_to "編集", edit_movie_path(@movie.id), class: "btn edit-btn" %>
<%= link_to "削除", destroy_movie_path(@movie.id), method: "delete", class: "btn destroy-btn", "data-confirm" => "本当に削除します？" %>